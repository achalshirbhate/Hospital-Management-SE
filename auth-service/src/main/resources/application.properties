spring.application.name=auth-service
server.port=${PORT:8081}
server.address=0.0.0.0

# ── Database (Supabase) ──
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.dkksbddtojuhhbdkyvad}
spring.datasource.password=${DB_PASSWORD:Ach@l789Achal}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=60000

# ── JPA ──
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# ── JWT ──
jwt.secret=${JWT_SECRET:TelePatientSuperSecretKeyForJWTAuthenticationSystem2024XYZ}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ── Email (Gmail SMTP) ──
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:achalshirbhate4@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jkapdddissiosacl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ── CORS ──
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
