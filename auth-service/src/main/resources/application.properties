# Application name
spring.application.name=auth-service

# Server port
server.port=8081

# Database configuration
# Using Supabase Session Pooler (IPv4 compatible)
# Username must be postgres.<project-ref> when using the pooler
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.dkksbddtojuhhbdkyvad
spring.datasource.password=Ach@l789Achal
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP - keep pool small for Supabase free tier
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=60000

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Email (Gmail SMTP) — replace with your Gmail + App Password
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=achalshirbhate4@gmail.com
spring.mail.password=jkapdddissiosacl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Static resource caching disabled (dev mode - changes reflect on Ctrl+Shift+R)
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false
